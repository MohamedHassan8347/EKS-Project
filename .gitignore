# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfplan

# Optional: override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# OS / editor
.DS_Store
.vscode/
github-actions-terraform-trust.json

# Local IAM trust policies / scratch
github-actions-terraform-trust.json

# Terraform
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.*
**/tfplan
**/*.tfplan
**/crash.log

# macOS
.DS_Store

# Local kube / helm caches
**/.kube/
**/.helm/

# Node
app/node_modules/


# Local helm/k8s working dirs if you don't want to commit them
# (REMOVE these two lines if you DO want them in GitOps)
# helm/monitoring/
# k8s/monitoring/

# Generated helm manifests can include Secrets
k8s/**/rendered-manifests*.yaml
k8s/**/manifest*.yaml

# local scratch
*.log
.DS_Store


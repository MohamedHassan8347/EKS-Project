controller:
  metrics:
    enabled: true

    service:
      enabled: true
      type: ClusterIP
      ports:
        metrics: 10254

    serviceMonitor:
      enabled: true
      namespace: monitoring
      additionalLabels:
        release: monitoring

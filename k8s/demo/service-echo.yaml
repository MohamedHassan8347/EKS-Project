apiVersion: v1
kind: Service
metadata:
  name: echo
  namespace: demo
  labels:
    app: echo
spec:
  selector:
    app: echo
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP

